package FoodDelivery;

public class Main {
    public static void main(String[] args) {
        //create customer
        //create driver
        //create restraunt
        //customer get List<Restraunt> strategy pattern, for now get nearest first
        //resturantService.getMenu(restId)
        //create Cart -> cart
        //cartService.add(Cart) or updateCart()
        //orderService.order(Cart)
        //restrauntService.acceptOrder();
        //driverService.assignDriver() // strategy pattern
        //driverService.pickupOrder();
        //driverService.deliverOrder();
    }
}
